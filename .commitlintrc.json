{
  "extends": ["@commitlint/config-conventional"],
  "rules": {
    "body-max-line-length": [2, "always", 120],
    "footer-max-line-length": [2, "always", 120],
    "scope-enum": [
      2,
      "always",
      ["frontend", "backend", "contracts", "root", "infra", "docs", "ci"]
    ]
  }
}
